.env
data/
out/
__pycache__/
*.pyc
installer/*.msi
installer/*.wixpdb
tray/bin/
tray/obj/
tray/publish/

# Local virtualenv (36 MB, machine-specific)
.venv/

# Token files: this one is an intentional empty placeholder, but a
# future non-empty version would be a credential.
*_hf_token
*.token
